# Add Sniper Code 2

## Implementation
- Add **Sniper Code 2** as one new game entry using the supplied playable URL.
- Keep the existing catalog, page layout, proxy behavior, and GitHub Pages setup unchanged.
- Store the game URL in the same encoded format as the rest of the catalog.
- Verify the title appears in search and opens in the existing game player.
